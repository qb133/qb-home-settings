# Skip the first-run welcome flow. Dismissing it is not enough: herdr treats a
# missing key as `true` ("Missing also shows onboarding" in its own generated
# config), so it has to be written out explicitly.
onboarding = false

[keys]
# `prefix+d` is unbound in herdr (only shift+d, close-workspace, is taken), so
# reclaim tmux's universal detach key. `prefix+q` stays valid so the built-in
# help and the docs are still telling the truth.
detach = ["prefix+d", "prefix+q"]

[ui]
show_agent_labels_on_pane_borders = false

# New tabs prompt for a name by default but new spaces do not, so prefix+shift+n
# would silently create a generated name while prefix+c asks. Make them match.
prompt_new_workspace_name = true

agent_panel_sort = "spaces"

[theme]
name = "catppuccin"
auto_switch = false

[ui.sound]
enabled = false
